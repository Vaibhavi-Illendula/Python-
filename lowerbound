def lowerbound(nums,x):
    n=len(arr)
    low=0
    high=n-1
    ans=n
    while low<=high:
        mid=(low+high)//2
        if nums[mid]==x:
            ans=mid
            high=mid-1
        elif nums[mid]<x:
            low=mid+1
        else:
            high=mid-1
            ans=mid
    return ans
arr=list(map(int,input().split()))  
x=int(input())
print(lowerbound(arr,x))
